name: Auto merge
on:
  push:
    branches:
      - '*'
      - '!main'
jobs:
  auto-merge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: patch-1
      - uses: actions/checkout@v2
        with:
          ref: main
      - name: print
        run: |
          git branch
          git log